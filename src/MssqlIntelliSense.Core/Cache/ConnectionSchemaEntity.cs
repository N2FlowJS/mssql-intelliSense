// ConnectionSchemaEntity has been removed.
// Schema metadata is now stored in dedicated relational tables (cache_tables, cache_views, etc.)
// schema_updated_at is stored directly in the connections table (ConnectionEntity.SchemaUpdatedAt).
